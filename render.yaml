services:
  - type: web
    name: helpix-backend
    env: python
    buildCommand: pip install uv && uv pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
